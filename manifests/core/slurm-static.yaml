apiVersion: v1
kind: Namespace
metadata:
  name: slurm
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slurm-ctld-state
  namespace: slurm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: slurm-controller
  namespace: slurm
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: slurm-controller
  ports:
    - name: slurmctld
      port: 6817
      targetPort: 6817
---
apiVersion: v1
kind: Service
metadata:
  name: slurm-restapi
  namespace: slurm
spec:
  selector:
    app: slurm-controller
  ports:
    - name: rest
      port: 6820
      targetPort: 6820
---
apiVersion: v1
kind: Service
metadata:
  name: slurm-worker-cpu
  namespace: slurm
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: slurm-worker-cpu
  ports:
    - name: slurmd
      port: 6818
      targetPort: 6818
---
apiVersion: v1
kind: Service
metadata:
  name: slurm-worker-gpu-a10
  namespace: slurm
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: slurm-worker-gpu-a10
  ports:
    - name: slurmd
      port: 6818
      targetPort: 6818
---
apiVersion: v1
kind: Service
metadata:
  name: slurm-worker-gpu-h100
  namespace: slurm
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: slurm-worker-gpu-h100
  ports:
    - name: slurmd
      port: 6818
      targetPort: 6818
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: slurm-config
  namespace: slurm
data:
  slurm.conf: |
    ClusterName=kind-slurm
    SlurmctldHost=slurm-controller-0(slurm-controller-0.slurm-controller.slurm.svc.cluster.local)
    MpiDefault=pmi2
    ProctrackType=proctrack/linuxproc
    ReturnToService=2
    SlurmctldPidFile=/var/run/slurmctld.pid
    SlurmdPidFile=/var/run/slurmd.pid
    SlurmdSpoolDir=/var/spool/slurmd
    SlurmUser=root
    StateSaveLocation=/var/spool/slurmctld
    SwitchType=switch/none
    TaskPlugin=task/none
    SchedulerType=sched/backfill
    MailProg=/usr/bin/true
    SelectType=select/cons_tres
    SelectTypeParameters=CR_Core

    SlurmctldPort=6817
    SlurmdPort=6818
    AuthType=auth/munge
    CryptoType=crypto/munge
    AuthAltTypes=auth/jwt
    AuthAltParameters=jwt_key=/slurm-secrets/jwt_hs256.key

    # Job accounting via slurmdbd (gracefully degrades if slurmdbd is unavailable)
    AccountingStorageType=accounting_storage/slurmdbd
    AccountingStorageHost=slurmdbd.slurm.svc.cluster.local
    AccountingStoragePort=6819
    JobAcctGatherType=jobacct_gather/linux
    JobAcctGatherFrequency=30
    GresTypes=gpu

    NodeName=slurm-worker-cpu-0 NodeAddr=slurm-worker-cpu-0.slurm-worker-cpu.slurm.svc.cluster.local NodeHostname=slurm-worker-cpu-0 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=cpu
    NodeName=slurm-worker-cpu-1 NodeAddr=slurm-worker-cpu-1.slurm-worker-cpu.slurm.svc.cluster.local NodeHostname=slurm-worker-cpu-1 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=cpu
    NodeName=slurm-worker-cpu-2 NodeAddr=slurm-worker-cpu-2.slurm-worker-cpu.slurm.svc.cluster.local NodeHostname=slurm-worker-cpu-2 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=cpu
    NodeName=slurm-worker-cpu-3 NodeAddr=slurm-worker-cpu-3.slurm-worker-cpu.slurm.svc.cluster.local NodeHostname=slurm-worker-cpu-3 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=cpu
    NodeName=slurm-worker-gpu-a10-0 NodeAddr=slurm-worker-gpu-a10-0.slurm-worker-gpu-a10.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-a10-0 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-a10 Gres=gpu:a10:1
    NodeName=slurm-worker-gpu-a10-1 NodeAddr=slurm-worker-gpu-a10-1.slurm-worker-gpu-a10.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-a10-1 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-a10 Gres=gpu:a10:1
    NodeName=slurm-worker-gpu-a10-2 NodeAddr=slurm-worker-gpu-a10-2.slurm-worker-gpu-a10.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-a10-2 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-a10 Gres=gpu:a10:1
    NodeName=slurm-worker-gpu-a10-3 NodeAddr=slurm-worker-gpu-a10-3.slurm-worker-gpu-a10.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-a10-3 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-a10 Gres=gpu:a10:1
    NodeName=slurm-worker-gpu-h100-0 NodeAddr=slurm-worker-gpu-h100-0.slurm-worker-gpu-h100.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-h100-0 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-h100 Gres=gpu:h100:1
    NodeName=slurm-worker-gpu-h100-1 NodeAddr=slurm-worker-gpu-h100-1.slurm-worker-gpu-h100.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-h100-1 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-h100 Gres=gpu:h100:1
    NodeName=slurm-worker-gpu-h100-2 NodeAddr=slurm-worker-gpu-h100-2.slurm-worker-gpu-h100.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-h100-2 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-h100 Gres=gpu:h100:1
    NodeName=slurm-worker-gpu-h100-3 NodeAddr=slurm-worker-gpu-h100-3.slurm-worker-gpu-h100.slurm.svc.cluster.local NodeHostname=slurm-worker-gpu-h100-3 CPUs=4 RealMemory=3500 Sockets=1 CoresPerSocket=2 ThreadsPerCore=2 State=UNKNOWN Feature=gpu,gpu-h100 Gres=gpu:h100:1
    PartitionName=debug Nodes=slurm-worker-cpu-0,slurm-worker-cpu-1,slurm-worker-cpu-2,slurm-worker-cpu-3,slurm-worker-gpu-a10-0,slurm-worker-gpu-a10-1,slurm-worker-gpu-a10-2,slurm-worker-gpu-a10-3,slurm-worker-gpu-h100-0,slurm-worker-gpu-h100-1,slurm-worker-gpu-h100-2,slurm-worker-gpu-h100-3 Default=YES MaxTime=INFINITE State=UP
  gres.conf: |
    NodeName=slurm-worker-gpu-a10-0 Name=gpu Type=a10 File=/dev/null
    NodeName=slurm-worker-gpu-a10-1 Name=gpu Type=a10 File=/dev/null
    NodeName=slurm-worker-gpu-a10-2 Name=gpu Type=a10 File=/dev/null
    NodeName=slurm-worker-gpu-a10-3 Name=gpu Type=a10 File=/dev/null
    NodeName=slurm-worker-gpu-h100-0 Name=gpu Type=h100 File=/dev/null
    NodeName=slurm-worker-gpu-h100-1 Name=gpu Type=h100 File=/dev/null
    NodeName=slurm-worker-gpu-h100-2 Name=gpu Type=h100 File=/dev/null
    NodeName=slurm-worker-gpu-h100-3 Name=gpu Type=h100 File=/dev/null
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: slurm-controller
  namespace: slurm
spec:
  serviceName: slurm-controller
  replicas: 1
  selector:
    matchLabels:
      app: slurm-controller
  template:
    metadata:
      labels:
        app: slurm-controller
    spec:
      containers:
        - name: slurm-controller
          image: slurm-controller:latest
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -lc
            - |
              set -euo pipefail
              MUNGE_SRC=/slurm-secrets/munge.key
              MUNGE_DST=/etc/munge/munge.key
              mkdir -p /etc/munge /run/munge /var/lib/munge /var/log/munge /run/sshd
              chown -R munge:munge /etc/munge /run/munge /var/lib/munge /var/log/munge
              chmod 0700 /etc/munge /var/lib/munge /var/log/munge
              chmod 0711 /run/munge
              install -o munge -g munge -m 0400 "$MUNGE_SRC" "$MUNGE_DST"
              ssh-keygen -A >/dev/null 2>&1 || true
              /usr/sbin/sshd
              su -s /bin/sh -c '/usr/sbin/munged --syslog' munge
              sleep 1
              pgrep -x munged >/dev/null
              # Wait for slurmdbd if accounting is configured, so slurmctld
              # does not fatal-exit on first boot due to missing TRES data.
              if grep -q 'AccountingStorageType=accounting_storage/slurmdbd' /etc/slurm/slurm.conf 2>/dev/null; then
                echo "[controller] waiting for slurmdbd port 6819..."
                until (echo >/dev/tcp/slurmdbd.slurm.svc.cluster.local/6819) 2>/dev/null; do
                  sleep 3
                done
                echo "[controller] slurmdbd is up"
              fi
              exec slurmctld -Dvvv &
              CTLD_PID=$!
              # Wait for slurmctld to be ready before starting slurmrestd.
              # slurmrestd needs SLURM_JWT set (from scontrol token) so that its
              # rest_auth/jwt plugin can bootstrap a valid auth context.
              until scontrol ping >/dev/null 2>&1; do sleep 2; done
              # lifespan=315360000 (10 years) avoids the daemon losing its auth
              # context after the default 24-hour token expiry.
              SLURM_JWT=$(scontrol token username=root lifespan=315360000 | sed 's/SLURM_JWT=//')                 SLURMRESTD_SECURITY=disable_user_check                 /usr/sbin/slurmrestd -a rest_auth/jwt -s openapi/v0.0.37 0.0.0.0:6820 &
              wait $CTLD_PID
          ports:
            - containerPort: 22
            - containerPort: 6817
            - containerPort: 6820
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmctld >/dev/null && pgrep -x munged >/dev/null"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmctld >/dev/null && pgrep -x slurmrestd >/dev/null"]
            initialDelaySeconds: 60
            periodSeconds: 20
          volumeMounts:
            - name: slurm-config
              mountPath: /etc/slurm
            - name: slurm-secrets
              mountPath: /slurm-secrets
              readOnly: true
            - name: ctld-state
              mountPath: /var/spool/slurmctld
            - name: shared-storage
              mountPath: /shared
      volumes:
        - name: slurm-config
          configMap:
            name: slurm-config
        - name: slurm-secrets
          projected:
            sources:
              - secret:
                  name: slurm-munge-key
                  items:
                    - key: munge.key
                      path: munge.key
              - secret:
                  name: slurm-ssh-key
                  items:
                    - key: id_ed25519
                      path: id_ed25519
                    - key: id_ed25519.pub
                      path: id_ed25519.pub
              - secret:
                  name: slurm-jwt-secret
                  items:
                    - key: jwt_hs256.key
                      path: jwt_hs256.key
        - name: ctld-state
          persistentVolumeClaim:
            claimName: slurm-ctld-state
        - name: shared-storage
          persistentVolumeClaim:
            claimName: slurm-shared-rwx
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: slurm-controller-pdb
  namespace: slurm
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: slurm-controller
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: slurm-worker-cpu
  namespace: slurm
spec:
  serviceName: slurm-worker-cpu
  replicas: 1
  selector:
    matchLabels:
      app: slurm-worker-cpu
  template:
    metadata:
      labels:
        app: slurm-worker-cpu
        worker-class: cpu-standard
    spec:
      containers:
        - name: slurm-worker
          image: slurm-worker:latest
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -lc
            - |
              set -euo pipefail
              MUNGE_SRC=/slurm-secrets/munge.key
              MUNGE_DST=/etc/munge/munge.key
              mkdir -p /etc/munge /run/munge /var/lib/munge /var/log/munge /run/sshd
              chown -R munge:munge /etc/munge /run/munge /var/lib/munge /var/log/munge
              chmod 0700 /etc/munge /var/lib/munge /var/log/munge
              chmod 0711 /run/munge
              install -o munge -g munge -m 0400 "$MUNGE_SRC" "$MUNGE_DST"
              ssh-keygen -A >/dev/null 2>&1 || true
              /usr/sbin/sshd
              su -s /bin/sh -c '/usr/sbin/munged --syslog' munge
              sleep 1
              pgrep -x munged >/dev/null
              exec slurmd -Dvvv -N "$(hostname)"
          ports:
            - containerPort: 22
            - containerPort: 6818
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmd >/dev/null && pgrep -x munged >/dev/null"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmd >/dev/null"]
            initialDelaySeconds: 20
            periodSeconds: 20
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - >-
                    scontrol update nodename=$(hostname) state=drain reason=k8s-eviction
                    2>/dev/null || true; sleep 10
          volumeMounts:
            - name: slurm-config
              mountPath: /etc/slurm
            - name: slurm-secrets
              mountPath: /slurm-secrets
              readOnly: true
            - name: shared-storage
              mountPath: /shared
            - name: modulefile-openmpi
              mountPath: /opt/modulefiles/openmpi
            - name: modulefile-python3
              mountPath: /opt/modulefiles/python3
            - name: modulefile-cuda
              mountPath: /opt/modulefiles/cuda
      volumes:
        - name: slurm-config
          configMap:
            name: slurm-config
        - name: slurm-secrets
          projected:
            sources:
              - secret:
                  name: slurm-munge-key
                  items:
                    - key: munge.key
                      path: munge.key
              - secret:
                  name: slurm-ssh-key
                  items:
                    - key: id_ed25519
                      path: id_ed25519
                    - key: id_ed25519.pub
                      path: id_ed25519.pub
        - name: shared-storage
          persistentVolumeClaim:
            claimName: slurm-shared-rwx
        - name: modulefile-openmpi
          configMap:
            name: slurm-modulefile-openmpi
            optional: true
        - name: modulefile-python3
          configMap:
            name: slurm-modulefile-python3
            optional: true
        - name: modulefile-cuda
          configMap:
            name: slurm-modulefile-cuda
            optional: true
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: slurm-worker-cpu-pdb
  namespace: slurm
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: slurm-worker-cpu
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: slurm-worker-gpu-a10
  namespace: slurm
spec:
  serviceName: slurm-worker-gpu-a10
  replicas: 0
  selector:
    matchLabels:
      app: slurm-worker-gpu-a10
  template:
    metadata:
      labels:
        app: slurm-worker-gpu-a10
        worker-class: gpu-a10
    spec:
      containers:
        - name: slurm-worker
          image: slurm-worker:latest
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -lc
            - |
              set -euo pipefail
              MUNGE_SRC=/slurm-secrets/munge.key
              MUNGE_DST=/etc/munge/munge.key
              mkdir -p /etc/munge /run/munge /var/lib/munge /var/log/munge /run/sshd
              chown -R munge:munge /etc/munge /run/munge /var/lib/munge /var/log/munge
              chmod 0700 /etc/munge /var/lib/munge /var/log/munge
              chmod 0711 /run/munge
              install -o munge -g munge -m 0400 "$MUNGE_SRC" "$MUNGE_DST"
              ssh-keygen -A >/dev/null 2>&1 || true
              /usr/sbin/sshd
              su -s /bin/sh -c '/usr/sbin/munged --syslog' munge
              sleep 1
              pgrep -x munged >/dev/null
              exec slurmd -Dvvv -N "$(hostname)"
          ports:
            - containerPort: 22
            - containerPort: 6818
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmd >/dev/null && pgrep -x munged >/dev/null"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmd >/dev/null"]
            initialDelaySeconds: 20
            periodSeconds: 20
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - >-
                    scontrol update nodename=$(hostname) state=drain reason=k8s-eviction
                    2>/dev/null || true; sleep 10
          volumeMounts:
            - name: slurm-config
              mountPath: /etc/slurm
            - name: slurm-secrets
              mountPath: /slurm-secrets
              readOnly: true
            - name: shared-storage
              mountPath: /shared
            - name: modulefile-openmpi
              mountPath: /opt/modulefiles/openmpi
            - name: modulefile-python3
              mountPath: /opt/modulefiles/python3
            - name: modulefile-cuda
              mountPath: /opt/modulefiles/cuda
      volumes:
        - name: slurm-config
          configMap:
            name: slurm-config
        - name: slurm-secrets
          projected:
            sources:
              - secret:
                  name: slurm-munge-key
                  items:
                    - key: munge.key
                      path: munge.key
              - secret:
                  name: slurm-ssh-key
                  items:
                    - key: id_ed25519
                      path: id_ed25519
                    - key: id_ed25519.pub
                      path: id_ed25519.pub
        - name: shared-storage
          persistentVolumeClaim:
            claimName: slurm-shared-rwx
        - name: modulefile-openmpi
          configMap:
            name: slurm-modulefile-openmpi
            optional: true
        - name: modulefile-python3
          configMap:
            name: slurm-modulefile-python3
            optional: true
        - name: modulefile-cuda
          configMap:
            name: slurm-modulefile-cuda
            optional: true
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: slurm-worker-gpu-a10-pdb
  namespace: slurm
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: slurm-worker-gpu-a10
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: slurm-worker-gpu-h100
  namespace: slurm
spec:
  serviceName: slurm-worker-gpu-h100
  replicas: 0
  selector:
    matchLabels:
      app: slurm-worker-gpu-h100
  template:
    metadata:
      labels:
        app: slurm-worker-gpu-h100
        worker-class: gpu-h100
    spec:
      containers:
        - name: slurm-worker
          image: slurm-worker:latest
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -lc
            - |
              set -euo pipefail
              MUNGE_SRC=/slurm-secrets/munge.key
              MUNGE_DST=/etc/munge/munge.key
              mkdir -p /etc/munge /run/munge /var/lib/munge /var/log/munge /run/sshd
              chown -R munge:munge /etc/munge /run/munge /var/lib/munge /var/log/munge
              chmod 0700 /etc/munge /var/lib/munge /var/log/munge
              chmod 0711 /run/munge
              install -o munge -g munge -m 0400 "$MUNGE_SRC" "$MUNGE_DST"
              ssh-keygen -A >/dev/null 2>&1 || true
              /usr/sbin/sshd
              su -s /bin/sh -c '/usr/sbin/munged --syslog' munge
              sleep 1
              pgrep -x munged >/dev/null
              exec slurmd -Dvvv -N "$(hostname)"
          ports:
            - containerPort: 22
            - containerPort: 6818
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmd >/dev/null && pgrep -x munged >/dev/null"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x slurmd >/dev/null"]
            initialDelaySeconds: 20
            periodSeconds: 20
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - >-
                    scontrol update nodename=$(hostname) state=drain reason=k8s-eviction
                    2>/dev/null || true; sleep 10
          volumeMounts:
            - name: slurm-config
              mountPath: /etc/slurm
            - name: slurm-secrets
              mountPath: /slurm-secrets
              readOnly: true
            - name: shared-storage
              mountPath: /shared
            - name: modulefile-openmpi
              mountPath: /opt/modulefiles/openmpi
            - name: modulefile-python3
              mountPath: /opt/modulefiles/python3
            - name: modulefile-cuda
              mountPath: /opt/modulefiles/cuda
      volumes:
        - name: slurm-config
          configMap:
            name: slurm-config
        - name: slurm-secrets
          projected:
            sources:
              - secret:
                  name: slurm-munge-key
                  items:
                    - key: munge.key
                      path: munge.key
              - secret:
                  name: slurm-ssh-key
                  items:
                    - key: id_ed25519
                      path: id_ed25519
                    - key: id_ed25519.pub
                      path: id_ed25519.pub
        - name: shared-storage
          persistentVolumeClaim:
            claimName: slurm-shared-rwx
        - name: modulefile-openmpi
          configMap:
            name: slurm-modulefile-openmpi
            optional: true
        - name: modulefile-python3
          configMap:
            name: slurm-modulefile-python3
            optional: true
        - name: modulefile-cuda
          configMap:
            name: slurm-modulefile-cuda
            optional: true
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: slurm-worker-gpu-h100-pdb
  namespace: slurm
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: slurm-worker-gpu-h100
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slurm-login
  namespace: slurm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slurm-login
  template:
    metadata:
      labels:
        app: slurm-login
    spec:
      containers:
        - name: slurm-login
          image: slurm-worker:latest
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -lc
            - |
              set -euo pipefail
              MUNGE_SRC=/slurm-secrets/munge.key
              MUNGE_DST=/etc/munge/munge.key
              SSH_SRC_DIR=/slurm-secrets
              if [[ ! -f "$MUNGE_SRC" ]]; then
                echo "[login] missing $MUNGE_SRC" >&2
                exit 1
              fi
              mkdir -p /etc/munge /run/munge /var/lib/munge /var/log/munge /run/sshd
              chown -R munge:munge /etc/munge /run/munge /var/lib/munge /var/log/munge
              chmod 0700 /etc/munge /var/lib/munge /var/log/munge
              chmod 0711 /run/munge
              cp "$MUNGE_SRC" "$MUNGE_DST"
              chown munge:munge "$MUNGE_DST"
              chmod 0400 "$MUNGE_DST"
              install -d -m 0700 /root/.ssh
              if [[ -f "$SSH_SRC_DIR/id_ed25519" ]]; then
                cp "$SSH_SRC_DIR/id_ed25519" /root/.ssh/id_ed25519
                chmod 0600 /root/.ssh/id_ed25519
              fi
              if [[ -f "$SSH_SRC_DIR/id_ed25519.pub" ]]; then
                cp "$SSH_SRC_DIR/id_ed25519.pub" /root/.ssh/id_ed25519.pub
                chmod 0644 /root/.ssh/id_ed25519.pub
                cat /root/.ssh/id_ed25519.pub >> /root/.ssh/authorized_keys
                chmod 0600 /root/.ssh/authorized_keys
              fi
              ssh-keygen -A >/dev/null 2>&1 || true
              if [[ -d /opt/slurm-runtime-src ]]; then
                install -d -m 0755 /opt/slurm-runtime
                cp -f /opt/slurm-runtime-src/* /opt/slurm-runtime/ 2>/dev/null || true
                chmod +x /opt/slurm-runtime/* 2>/dev/null || true
              fi
              if ! su -s /bin/sh -c '/usr/sbin/munged --syslog' munge; then
                echo "[login] munged failed to start" >&2
                exit 1
              fi
              sleep 1
              pgrep -x munged >/dev/null
              /usr/sbin/sshd
              echo "[login] ready: munged+sshd running"
              tail -f /dev/null
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -x munged >/dev/null"]
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: slurm-config
              mountPath: /etc/slurm
            - name: slurm-secrets
              mountPath: /slurm-secrets
              readOnly: true
            - name: slurm-ddp-runtime
              mountPath: /opt/slurm-runtime-src
              readOnly: true
            - name: shared-storage
              mountPath: /shared
            - name: modulefile-openmpi
              mountPath: /opt/modulefiles/openmpi
            - name: modulefile-python3
              mountPath: /opt/modulefiles/python3
            - name: modulefile-cuda
              mountPath: /opt/modulefiles/cuda
      volumes:
        - name: slurm-config
          configMap:
            name: slurm-config
        - name: slurm-secrets
          projected:
            sources:
              - secret:
                  name: slurm-munge-key
                  items:
                    - key: munge.key
                      path: munge.key
              - secret:
                  name: slurm-ssh-key
                  items:
                    - key: id_ed25519
                      path: id_ed25519
                    - key: id_ed25519.pub
                      path: id_ed25519.pub
        - name: slurm-ddp-runtime
          configMap:
            name: slurm-ddp-runtime
            defaultMode: 0755
        - name: shared-storage
          persistentVolumeClaim:
            claimName: slurm-shared-rwx
        - name: modulefile-openmpi
          configMap:
            name: slurm-modulefile-openmpi
            optional: true
        - name: modulefile-python3
          configMap:
            name: slurm-modulefile-python3
            optional: true
        - name: modulefile-cuda
          configMap:
            name: slurm-modulefile-cuda
            optional: true
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: slurm-login-pdb
  namespace: slurm
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: slurm-login
---
apiVersion: v1
kind: Service
metadata:
  name: slurm-login
  namespace: slurm
spec:
  selector:
    app: slurm-login
  ports:
    - name: ssh
      port: 22
      targetPort: 22
